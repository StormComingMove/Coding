#include <iostream>
#include <vector>
#include <map>
#include <unordered_map>
using namespace std;

int main(void){
	int in;
	cin >> in;
	int x = 0;
	vector<int> v;
	for(int i = 0;i < in;i++)
		v.push_back(i);
	return 0;
}
